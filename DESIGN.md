# AI Cove Turbo 桌面应用设计约束

## 1. Atmosphere & Identity

桌面应用采用已确认的最新原型作为唯一视觉基线：实时页和统计页复用 C 的高密度运维控制台，配置页复用横向 B 面板。三个页面共用 Dot Field 背景，但各自保持明确的功能边界。

目标用户是需要低存在感配置入口的普通 Codex 用户，以及需要快速扫读路由、压缩与连接状态的技术用户。两类用户都必须能通过键盘完成 Tab 切换并清楚区分“配置动作”和“只读运行观测”。

## 2. Color

- B 基础色保持：`--b-bg #0b1010`、`--b-surface #151c1b`、`--b-surface-2 #1c2523`、`--b-ink #eff5ef`。
- C 基础色保持：`--c-bg #080c0b`、`--c-panel #101613`、`--c-panel-2 #141c18`、`--c-ink #e6f0e8`。
- 旧 B/C 的绿色强调统一替换为 AI Cove 海蓝：`--b-accent` 与 `--c-accent` 均为 `#70d8ee`；对应透明高光使用 `rgba(112, 216, 238, ...)`。
- 三页外壳使用独立 `--turbo-*` 语义令牌管理颜色、间距、字号、圆角、焦点和图标尺寸，不把外壳数值散落到组件规则中。
- 暖色告警和深色层级保持不变；中性色仅允许为 WCAG AA 可读性微调语义 `muted`，不引入新的渐变体系。

## 3. Typography

- 完整保留旧 B/C 的系统无衬线与系统等宽字体栈。
- 保留旧版字重、字距、英文大写运维标签和数据数字样式；关键状态、Tab 与筛选标签不得低于约 12px。
- 中文状态文案避免单字孤行；路由与长字符串允许安全换行或省略。

## 4. Spacing & Layout

- 外层为 58px 品牌/Tab 栏加一个可滚动内容区，页面顺序固定为实时、统计、配置。
- 外层是主要品牌层；实时与统计内页不重复 Turbo 图标、产品名与版本，配置页仅在主卡片上方保留紧凑的版本与更新栏。
- 配置页使用最大宽度 980px 的横向 B 面板，左侧展示生效链路，右侧集中配置动作。
- 实时页只承载运行状态和最近 100 条真实请求；统计页只承载聚合指标、筛选、时间轴和滚动窗口。
- 实时页只在 Codex 明确待重启时显示就地重启动作；配置开关和其余配置动作只在配置页出现，统计页保持只读。
- 实时页的 WebSocket 连接检查器以贴住导航下边框的居中摘要标签为折叠态，展开后仍从同一位置向下承载非模态面板，不使用遮罩；面板可由底部阻力条水平移动但不得越出窗口，默认最多占到页面中段并保留可见的最新传输日志。
- 桌面低高度窗口中实时状态列允许独立纵向滚动，HTTP 与 WebSocket 三项核心状态必须始终可达。
- 375 / 768 / 1280px 均不得产生整页横向滚动。

## 5. Components

- `TurboShellHeader`：Turbo 图标、产品名、实时/统计/配置 Tab、预览标识；macOS 使用原生 `Overlay` 标题栏，把红黄绿按钮纳入导航层并隐藏独立窗口标题，导航空白区域可拖拽，左侧品牌为原生按钮保留安全间距。
- `Tabs`：原生按钮与 `tablist/tab/tabpanel` 语义，支持点击、左右方向键、Home、End；选中态由单一海蓝指示层在三个位置间平滑移动，按钮本身只承载文字与交互状态。
- `VersionUpdateBar`：恢复旧版运维顶栏的全宽样式，紧贴顶部导航栏下方，集中展示当前版本、更新状态、检查/安装动作与条件式进度。
- `BPopover`：状态、压缩与 WebSocket 独立开关、会话摘要、自启动与重启 Codex；运行偏好开关使用统一线性 SVG 图标表达压缩、双向连接、电源启动与 Dock；公网腿 zstd 复用 WebSocket 自动协商，不新增第三个开关。
- `LiveConsole`：只读路由、实时健康状态和最近 100 条真实请求；失败状态提供原因、下一步与可选技术详情，Codex 待重启时提供一次就地修复入口。终端默认跟随最新请求，用户上滚超过底部 24px 后暂停跟随并累计新请求数，点击“回到最新”或手动滚回底部后恢复；该状态不影响“暂停请求流”。
- `LiveRequestPulse`：新请求只增量追加到终端；最新行与当前可见的 `StatusStrands` 同步产生一次短促能量脉冲，强度由该请求的真实节省率驱动，不为轮询或历史数据重放动画。
- `StatisticsConsole`：按时间范围、传输方式和结果筛选进程内聚合；固定使用 6×10 秒、10×1 分钟、12×5 分钟、24×1 小时滚动桶；速度收益仅作为次级“基准模型估算”，不得与真实字节指标同级展示。
- `TurboIcon`：使用 `assets/turbo-icon.png`，只作为品牌标识，不替代真实交互控件。
- `StatusStrands`：配置页主状态右侧与实时页“实时状态”标题右侧复用同一套 React Bits Strands WebGL2 波形、加法混色和指数 bloom；实时页使用约 96×64px 的紧凑画布。两个实例都由服务、配置、Codex、HTTP、WebSocket 五项聚合状态中已验证的数量驱动，范围固定为 0–5。
- `ConnectionInspector`：导航下方摘要标签以发送、接收、休眠三类图标和数量持续显示绑定连接状态；摘要标签与实时页 `StatusStrands` 都可切换面板。分组顺序固定为预热池、绑定会话、建立 / 恢复中、近期关闭。完整 `thread_id` 作为会话身份并显示进程内稳定、可回收的 `会话 01` 短号；同一会话内以后端观察连接 ID 分配只递增的 `连接 01` 短号，绑定、恢复和近期关闭沿用同一身份。会话首次出现时异步读取一次 Codex 本地会话名称并按 `thread_id` 缓存；读取失败不得阻塞连接快照或重复轮询。会话 hover / focus 详情显示“会话名称”和完整“会话 ID”。会话组使用圆角矩形对话气泡图标，真实连接保留圆点图标；近期关闭中的会话复用当前绑定或恢复状态色，完全释放后统一为灰色，红色只表示具体异常连接。绑定会话使用双列布局；每个会话常驻显示 `会话 01 ×2`、分隔符及按稳定编号排序并等分剩余宽度的连接标签，1 条连接显示完整名称，2 条只显示编号，3 条以上只保留状态图标。近期关闭保持单列，并仅在真实可用宽度不足时按“完整名称 → 编号 → 状态图标”逐级降密度。会话和每个连接仍可通过 hover / focus 查看完整详情，不再显示聚合传输图标或图钉。建立 / 恢复项不按会话分组，每条连接独立平铺为 `会话 01 · 连接 03`，展开后展示操作、连接身份、用时和详情；池级操作继续保持独立。近期关闭最多保留 8 个关闭事件，每个事件分别保留原因、结果和时间。预热连接继续显示 `P01`，不进入会话编号体系；预热空白为灰色，传输中为绿色，绑定空闲为蓝色，状态仍须附带文字和可访问名称。
- `ConnectionReclaimCopy`：只陈述真实策略。绑定空闲连接显示“随线程结束回收”，预热连接显示“容量压力时回收”；在后端没有固定超时前不得显示倒计时或预计回收时间。

## 6. Motion & Interaction

- 保留旧 B/C 的 hover、focus、pressed 反馈。
- Tab 海蓝选中层使用 `--speed-standard`（240ms）和 `--ease-out` 平滑移动；页面按导航方向从左右 10px 进入，同时由 3px 模糊恢复，时长 180ms，连续切换立即取消旧动画，不等待退场。
- 恢复提示、更新安装等条件区域只在由隐藏变为可见时执行 140ms 淡入；轮询刷新不得重复播放。
- 实时请求脉冲固定为一次约 360ms 的到达反馈，只作用于新增行和小尺寸 Strands；相同请求数据不得重放，页面不可见或 `prefers-reduced-motion: reduce` 时不执行。
- 图表只保留一个 Tab 停靠点，使用左右方向键及 Home / End 浏览时间桶，避免 24 个连续焦点。
- 配置动作通过 Tauri 命令更新真实运行状态；WebSocket 开关同时更新受管 Provider 的 `supports_websockets`，并提示重启 Codex Desktop 后验证。
- 实时页可见时，连接摘要随 1 秒状态轮询读取内存快照；切出实时页或文档隐藏后停止读取。展开面板复用同一快照，刷新必须保留用户已展开的恢复详情，不能因轮询反复折叠。
- 连接面板以导航下方摘要为原点使用 transform、opacity 与 clip-path 平滑展开；底部阻力条支持点击或向上拖动收起，以及水平拖动整个面板。窄窗口把水平偏移钳制为零；键盘仍可收起和移动，`prefers-reduced-motion: reduce` 下立即切换。
- 连接检查器按会话、连接、恢复项和关闭事件的稳定身份原位更新 DOM；轮询不得重建仍存在的节点。文字变化使用轻微淡换，新增与移除使用短促淡入淡出，CSS 状态动画在快照刷新前后连续播放。
- `StatusStrands` 参数沿用参考默认质感：低速、双频波、0.7 厚度、2.6 bloom、3.0 边缘收束、1.875 缩放、高饱和及 0.4 强度；画布额外保留上下光晕缓冲，状态块纵向 padding 为 0，由 Canvas 直接决定区域高度；常规模式持续缓慢流动，`prefers-reduced-motion: reduce` 下冻结为静态波形。
- `prefers-reduced-motion: reduce` 下禁用页面与条件区域的 WAAPI 动画，并将 CSS 交互过渡缩短为近即时。
- 性能预算：终端按请求增量追加并保留最近 100 条，相同数据不得重建 DOM；实时与统计只渲染当前页面；文档隐藏时暂停 1 秒状态轮询与 Strands 动画帧，恢复可见后立即刷新一次。

## 7. Depth & Surface

- B 保留横向面板、圆角和阴影；配置主卡片背景保持 60% 不透明度，让 Dot Field 可见但不影响内容清晰度。
- C 保留全宽运维网格、线框分区和等宽数据层级。
- 外层 Tab 栏只使用低对比边线与轻微模糊，不覆盖或改写内层材质。

## 8. Accessibility Constraints & Accepted Debt

- 状态不能只靠颜色表达；所有开关保留文字状态与 `aria-pressed`。
- Tab 焦点顺序、选中状态和面板关联必须可被键盘与辅助技术识别。
- 已知错误优先显示用户可执行的恢复文案，底层异常只放入可展开技术详情。
- 连接检查器无焦点陷阱；摘要标签、`StatusStrands`、关闭按钮、底部阻力条和 `Escape` 均可切换或关闭，关闭后把焦点还给打开它的入口。读取失败信息位于核心连接分组之后，不得挤占面板顶部。
- Turbo 图标在已有文字品牌旁作为装饰图，使用空替代文本。
- 已接受债务：实时请求和统计只保留当前进程内存，不做历史数据库；速度提升按压缩字节、HTTP/WS 请求比例和阶段性基准常量估算，常量随后续基准结果校准，不等同于单次模型实测。
- 已接受债务：AI Cove private WebSocket 按上行消息实时记录；标准 WebSocket 隧道不解帧，仅在通道关闭时汇总真实上行字节，避免改变透传协议语义。
- 已接受债务：B/C 继承原型的局部颜色、间距和字号字面量，本票为保持用户确认的原样式不做全量令牌化；新增或调整的外壳与海蓝强调色必须使用语义令牌。
